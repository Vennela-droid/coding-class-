import java.util.*;
class Map
{
public static void main(String args[])
   {
     
     HashMap<Integer,String> a=new HashMap<Integer,String>();
     Scanner sc=new Scanner(System.in);
     int n;
     int key;
     String value;
     System.out.print("Enter n value:");
     n=sc.nextInt();
     for(int i=0;i<n;i++)
     {
       key=sc.nextInt();
       value=sc.next();
       a.put(key,value);
     }
      TreeMap<Integer,String> t=new TreeMap<Integer,String>(a);
      System.out.println(t);
   }
}
